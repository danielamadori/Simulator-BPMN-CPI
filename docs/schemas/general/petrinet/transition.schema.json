{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "transition.schema.json",
  "title": "Transition",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "label": { "type": "string" },
    "region_id": {
      "type": "string",
      "description": "Identifier of the region this transition belongs to."
    },
    "region_type": {
      "type": "string",
      "enum": [
        "choice",
        "nature",
        "parallel",
        "sequence",
        "loop"
      ],
      "description": "Type of region this transition belongs to, either 'region' or 'subregion'."
    },
    "probability": {
      "type": "number",
      "description": "Probability of this transition occurring, if applicable.",
      "minimum": 0,
      "maximum": 1
    },
    "stop": {
      "type": "boolean",
      "description": "Indicates if this transition stop execution."
    }
  },
  "required": ["id"],
  "additionalProperties": false
}