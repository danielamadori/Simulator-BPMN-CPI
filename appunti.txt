## Da aggiungere

(?) Manager versionamento dell'api
Manager dei contesti
# Startegy Pattern per il Controller
# Contesto(Immutabile) che contiene DataSpin, Stack(Memento), Strategia(Controller)
# Classe DataMarking per Stack che contiene Marking(PM4PY) e age 
# Spostare age da DataSpin a DataMarking
Aggiungere impatti e probabilità cumulativa a DataMarking
Fare check di validità dei valori del json

## Da Correggere
traduzione a pnml

## Da controllare
Classi inutili di conversione

## API TYPES LAYOUT
il client deve gestire un bpmn, il server una rete di petri temporizzata (spin).
Il server deve implementare un convertitore che gestisca tale comunicazione.
Le regioni sono SESE, pertanto il MEMENTO nel Client dovrà gestire un marking di bpmn.
Domanda -> esiste la possibilità che la rete faccia una consume e non cambi nulla dal pdv del bpmn?
Risposta -> Teoricamente no, poichè è sese.
Ergo si può fare un memento con id regioni se attive o meno e costo complessivo accumulato e tempo trascorso.
-> creare unn LOG 

Problema: Se gestisco il memento con id regioni associa ad ogni regione la sua age?
-> 1 regione = 1 place.
Se è sequenziale (non dovrebbe essere un problema)
